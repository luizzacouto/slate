<h1 id='cupons'>Cupons</h1><h2 id='listar-cupons'><img src="/images/get.png"> Listar cupons</h2>
<p>Get a list of coupons</p>
<h3 id='http-request'>HTTP Request</h3>
<p><code>GET /products/:product_id/coupons</code></p>
<pre class="highlight ruby tab-ruby"><code><span class="no">EXEMPLO</span> <span class="no">DE</span> <span class="no">CHAMADA</span> <span class="no">UTILIZANDO</span> <span class="no">RUBY</span>
</code></pre><pre class="highlight python tab-python"><code><span class="n">EXEMPLO</span> <span class="n">DE</span> <span class="n">CHAMADA</span> <span class="n">UTILIZADO</span> <span class="n">PHYTON</span>
</code></pre><pre class="highlight javascript tab-javascript"><code><span class="nx">EXEMPLO</span> <span class="nx">DE</span> <span class="nx">CHAMADA</span> <span class="nx">UTILIZANDO</span> <span class="nx">JS</span>
</code></pre>
<blockquote>
<p>The above command returns JSON structured like this:</p>
</blockquote>
<pre class="highlight json tab-json"><code><span class="p">[</span><span class="w">
  </span><span class="err">EXEMPLO</span><span class="w"> </span><span class="err">DE</span><span class="w"> </span><span class="err">JSON</span><span class="w">
</span><span class="p">]</span><span class="w">
</span></code></pre><h3 id='parametros-obrigatorios'>Parâmetros obrigatórios</h3>
<table><thead>
<tr>
<th>Nome</th>
<th>Tipo</th>
<th>Descrição</th>
</tr>
</thead><tbody>
<tr>
<td>product_id</td>
<td>Integer</td>
<td>The id of the product</td>
</tr>
</tbody></table>
<h2 id='criar-cupom'><img src="/images/post.png"> Criar cupom</h2>
<p>Create new coupon</p>
<h3 id='http-request-2'>HTTP Request</h3>
<p><code>POST /coupons</code></p>
<pre class="highlight ruby tab-ruby"><code><span class="no">EXEMPLO</span> <span class="no">DE</span> <span class="no">CHAMADA</span> <span class="no">UTILIZANDO</span> <span class="no">RUBY</span>
</code></pre><pre class="highlight python tab-python"><code><span class="n">EXEMPLO</span> <span class="n">DE</span> <span class="n">CHAMADA</span> <span class="n">UTILIZADO</span> <span class="n">PHYTON</span>
</code></pre><pre class="highlight javascript tab-javascript"><code><span class="nx">EXEMPLO</span> <span class="nx">DE</span> <span class="nx">CHAMADA</span> <span class="nx">UTILIZANDO</span> <span class="nx">JS</span>
</code></pre>
<blockquote>
<p>The above command returns JSON structured like this:</p>
</blockquote>
<pre class="highlight json tab-json"><code><span class="p">[</span><span class="w">
  </span><span class="err">EXEMPLO</span><span class="w"> </span><span class="err">DE</span><span class="w"> </span><span class="err">JSON</span><span class="w">
</span><span class="p">]</span><span class="w">
</span></code></pre><h3 id='parametros-obrigatorios-2'>Parâmetros obrigatórios</h3>
<table><thead>
<tr>
<th>Nome</th>
<th>Tipo</th>
<th>Descrição</th>
</tr>
</thead><tbody>
<tr>
<td>coupon</td>
<td>Hash</td>
<td>The coupon to create</td>
</tr>
</tbody></table>
<h2 id='atualizar-um-cupom'><img src="/images/put.png"> Atualizar um cupom</h2>
<p>Update an existing coupon and redirect to &#39;show&#39; action</p>
<h3 id='http-request-3'>HTTP Request</h3>
<p><code>PUT /coupon/:id</code></p>
<pre class="highlight ruby tab-ruby"><code><span class="no">EXEMPLO</span> <span class="no">DE</span> <span class="no">CHAMADA</span> <span class="no">UTILIZANDO</span> <span class="no">RUBY</span>
</code></pre><pre class="highlight python tab-python"><code><span class="n">EXEMPLO</span> <span class="n">DE</span> <span class="n">CHAMADA</span> <span class="n">UTILIZADO</span> <span class="n">PHYTON</span>
</code></pre><pre class="highlight javascript tab-javascript"><code><span class="nx">EXEMPLO</span> <span class="nx">DE</span> <span class="nx">CHAMADA</span> <span class="nx">UTILIZANDO</span> <span class="nx">JS</span>
</code></pre>
<blockquote>
<p>The above command returns JSON structured like this:</p>
</blockquote>
<pre class="highlight json tab-json"><code><span class="p">[</span><span class="w">
  </span><span class="err">EXEMPLO</span><span class="w"> </span><span class="err">DE</span><span class="w"> </span><span class="err">JSON</span><span class="w">
</span><span class="p">]</span><span class="w">
</span></code></pre><h3 id='parametros-obrigatorios-3'>Parâmetros obrigatórios</h3>
<table><thead>
<tr>
<th>Nome</th>
<th>Tipo</th>
<th>Descrição</th>
</tr>
</thead><tbody>
<tr>
<td>id</td>
<td>Integer</td>
<td>The id of the coupon to update</td>
</tr>
<tr>
<td>coupon</td>
<td>Hash</td>
<td>The coupon to update</td>
</tr>
</tbody></table>
<h2 id='excluir-um-cupom'><img src="/images/delete.png"> Excluir um cupom</h2>
<p>Destroy a coupon if unused</p>
<h3 id='http-request-4'>HTTP Request</h3>
<p><code>DELETE /coupon/:id</code></p>
<pre class="highlight ruby tab-ruby"><code><span class="no">EXEMPLO</span> <span class="no">DE</span> <span class="no">CHAMADA</span> <span class="no">UTILIZANDO</span> <span class="no">RUBY</span>
</code></pre><pre class="highlight python tab-python"><code><span class="n">EXEMPLO</span> <span class="n">DE</span> <span class="n">CHAMADA</span> <span class="n">UTILIZADO</span> <span class="n">PHYTON</span>
</code></pre><pre class="highlight javascript tab-javascript"><code><span class="nx">EXEMPLO</span> <span class="nx">DE</span> <span class="nx">CHAMADA</span> <span class="nx">UTILIZANDO</span> <span class="nx">JS</span>
</code></pre>
<blockquote>
<p>The above command returns JSON structured like this:</p>
</blockquote>
<pre class="highlight json tab-json"><code><span class="p">[</span><span class="w">
  </span><span class="err">EXEMPLO</span><span class="w"> </span><span class="err">DE</span><span class="w"> </span><span class="err">JSON</span><span class="w">
</span><span class="p">]</span><span class="w">
</span></code></pre><h3 id='parametros-obrigatorios-4'>Parâmetros obrigatórios</h3>
<table><thead>
<tr>
<th>Nome</th>
<th>Tipo</th>
<th>Descrição</th>
</tr>
</thead><tbody>
<tr>
<td>id</td>
<td>Integer</td>
<td>The id of the coupon to destroy</td>
</tr>
</tbody></table>
